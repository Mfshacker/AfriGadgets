# AfriGadgets browser-tab logo / favicon

The browser tab icon is `images/branding/afrigadgets-favicon.svg` with a PNG fallback at `images/branding/afrigadgets-favicon.png`. All HTML pages include icon links in their `<head>`. To replace it later, swap those two files while keeping the filenames, or update the icon links.
