# CSS

All project stylesheets live under this folder.

- `storefront/` contains shared storefront styles and page-specific styles.
- `pages/` contains the separate Contact page theme.
- `admin/` contains admin dashboard styles.

See `storefront/README.md` for the main stylesheet loading rules.
