# Product Images

Catalogue images belong here. Product filenames are referenced by `js/shared/products.js`.

Use lowercase, descriptive filenames and keep each file under version control. The catalogue currently references more product images than are present in this folder; the storefront falls back to a Font Awesome icon when an image cannot be loaded.
