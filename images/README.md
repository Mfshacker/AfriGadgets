# Images

Site images are grouped by use:

- `hero/` contains homepage hero images.
- `products/` contains catalogue product images.

Use descriptive filenames and keep product image filenames in sync with the `image` values in `js/shared/products.js`.
